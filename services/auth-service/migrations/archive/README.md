# Archived manual SQL

These files were applied ad hoc before **`node-pg-migrate`** was added. The canonical schema is now **`../db/migrations/`** (see auth-service README: `npm run migrate:up`).

Do not re-run these on a database that already matches the current schema.
